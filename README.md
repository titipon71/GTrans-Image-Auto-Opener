![alt text](icon.png)

# Google Translate Image Auto-Opener

**Google Translate Image Auto-Opener** เป็นส่วนเสริมของ Google Chrome (Chrome Extension) ที่ช่วยอำนวยความสะดวกในการแปลรูปภาพบน Google Translate โดยจะทำการเปิดรูปภาพที่แปลเสร็จแล้วในแท็บใหม่ให้ทันที และสามารถตั้งค่าให้ Redirect กลับมาหน้าเดิมเพื่อเตรียมแปลรูปต่อไปได้โดยอัตโนมัติ

## ✨ ฟีเจอร์หลัก (Features)

* **Auto Open:** ตรวจจับเมื่อ Google Translate แปลรูปภาพเสร็จสิ้น (ที่เป็น `blob:` url) และเปิดรูปภาพนั้นในแท็บใหม่ทันที ไม่ต้องกดเซฟหรือกดเปิดเอง
* **Filter Logic:** มีระบบกรองรูปภาพขนาดเล็ก (กว้างหรือสูงน้อยกว่า 300px) เพื่อป้องกันการเปิดรูป icon หรือ thumbnail ที่ไม่เกี่ยวข้อง
* **Auto Redirect:** (เลือกเปิด/ปิดได้) หลังจากเปิดรูปภาพแล้ว สามารถสั่งให้หน้าเว็บ Redirect กลับไปยังหน้าอัปโหลดรูปภาพของ Google Translate ได้ทันที เพื่อความรวดเร็วในการแปลรูปถัดไป
* **Control Popup:** มีหน้าต่าง Popup สำหรับเปิด-ปิดการทำงานของส่วนเสริมได้ง่ายๆ พร้อมสถานะแจ้งเตือน

## 🛠 การติดตั้ง (Installation)

เนื่องจากส่วนเสริมนี้ยังไม่ได้อยู่บน Chrome Web Store คุณต้องติดตั้งแบบ **Unpacked** (โหมดนักพัฒนา) ดังนี้:

1. ดาวน์โหลดหรือ Clone โปรเจกต์นี้ลงในคอมพิวเตอร์ของคุณ
2. เปิด Google Chrome และไปที่ `chrome://extensions/`
3. เปิดสวิตช์ **"Developer mode" (โหมดนักพัฒนาซอฟต์แวร์)** ที่มุมขวาบน
4. คลิกปุ่ม **"Load unpacked" (โหลดส่วนขยายที่คลายแพ็กแล้ว)** ที่มุมซ้ายบน
5. เลือกโฟลเดอร์ที่เก็บไฟล์โปรเจกต์นี้ (`manifest.json` อยู่ในโฟลเดอร์นั้น)

## 📖 วิธีใช้งาน (Usage)

1. หลังจากติดตั้งเสร็จ ให้ไปที่เว็บไซต์ [Google Translate (โหมดรูปภาพ)](https://translate.google.co.th/?hl=th&sl=en&tl=th&op=images)
2. อัปโหลดรูปภาพที่ต้องการแปล
3. เมื่อแปลเสร็จสิ้น ส่วนเสริมจะทำงานดังนี้:
* เปิดรูปภาพผลลัพธ์ใน **แท็บใหม่**
* (ถ้าเปิด Redirect ไว้) แท็บเดิมจะโหลดหน้า Google Translate ใหม่เพื่อเคลียร์รูปเก่า


4. **การตั้งค่า:** คลิกที่ไอคอน Extension บน Toolbar เพื่อเปิดเมนู:
* **เปิดใช้งานหลัก (Auto Open):** เปิด/ปิด การทำงานทั้งหมด
* **Redirect อัตโนมัติ:** เปิด/ปิด การรีเฟรชหน้าเว็บหลังเปิดรูปเสร็จ
* *หมายเหตุ: หากมีการเปลี่ยนการตั้งค่า แนะนำให้ Reload หน้าเว็บ Google Translate หนึ่งครั้งเพื่อให้ค่าใหม่มีผล*



## 📂 โครงสร้างไฟล์ (File Structure)

* `manifest.json`: ไฟล์ระบุการตั้งค่าและสิทธิ์ของ Extension (Manifest V3)
* `content.js`: สคริปต์หลักที่ทำงานบนหน้าเว็บ Google Translate คอยตรวจจับรูปภาพและสั่ง Redirect
* `popup.html` & `popup.js`: ส่วนติดต่อผู้ใช้ (UI) สำหรับตั้งค่าเปิด-ปิดฟีเจอร์
* `icon.png`: ไอคอนของส่วนเสริม

## ⚠️ ข้อควรระวัง

* ส่วนเสริมนี้รองรับเฉพาะโดเมน `translate.google.co.th` และ `translate.google.com`
* การ Redirect อัตโนมัติถูกตั้งค่าไว้ที่ 500ms หลังจากเปิดรูปภาพ

---
